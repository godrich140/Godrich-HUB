# 装箱单项目测试报告

测试日期：2026-05-22  
测试对象：`http://129.204.142.19/`、本地前端代码 `frontend/`  
测试工具：Codex Browser、Vite、TypeScript Compiler API

## 测试结论

1. 本次已完成前端代码修改：在“明细录入”主操作区和“弹出明细录入”窗口增加“清空明细录入”按钮。
2. 本地 TypeScript 诊断通过，诊断数量为 0。
3. 本地 Vite 构建通过。
4. Browser 访问 `http://129.204.142.19/` 被企业网络策略拦截，无法执行远程站点浏览器走查。
5. Browser 访问本地开发地址 `http://127.0.0.1:5174/` 也被同一网络策略拦截，无法执行截图和交互验证。

## 已执行测试

| 编号 | 测试项 | 操作 | 结果 |
| --- | --- | --- | --- |
| T-001 | TypeScript 静态诊断 | 使用 TypeScript Compiler API 读取 `frontend/tsconfig.json` 并执行 `getPreEmitDiagnostics` | 通过，诊断数量 0 |
| T-002 | 前端构建 | 使用 Vite API 对 `frontend/` 执行构建 | 通过 |
| T-003 | 远程站点访问 | 使用 Browser 打开 `http://129.204.142.19/` | 阻塞，企业网络策略禁止访问 |
| T-004 | 本地站点访问 | 启动 Vite 服务后使用 Browser 打开 `http://127.0.0.1:5174/` | 阻塞，企业网络策略禁止访问 |

## 代码变更验证

| 功能点 | 验证方式 | 结果 |
| --- | --- | --- |
| 主表格清空按钮 | 检查 `WorkbenchPage` 操作区存在“清空明细录入”按钮，并绑定 `onClearRows` | 通过 |
| 弹窗清空按钮 | 检查 `ResizableDetailModal` 操作区存在“清空明细录入”按钮，并绑定同一 `onClearRows` | 通过 |
| 清空行为 | 检查 `clearRows()` 会弹出确认框，确认后将明细重置为 1 条空白行，并清空选中行 | 通过 |
| 样式 | 检查新增 `.danger-button` 样式，按钮语义与清空操作一致 | 通过 |

## 待执行浏览器用例

以下用例需要在 Browser/Chrome 可以访问目标地址后复测。

| 编号 | 模块 | 用例 | 预期结果 |
| --- | --- | --- | --- |
| U-001 | 工作台 | 打开首页，检查“工作台”默认选中 | 显示装箱单工作台、客户、日期、明细录入和照片识别区域 |
| U-002 | 工作台 | 点击“新增一行” | 明细行数增加 1，新增行可录入 |
| U-003 | 工作台 | 修改第一行品名、数量、单位、单价、总价 | 表格输入值保留，汇总数量和金额同步变化 |
| U-004 | 工作台 | 勾选连续多行后点击“合并选中行” | 箱规相关列纵向合并，选中状态清空 |
| U-005 | 工作台 | 勾选已合并行后点击“取消合并” | 合并状态取消，表格恢复普通行显示 |
| U-006 | 工作台 | 点击“清空明细录入”，确认弹窗选择取消 | 明细数据不变化 |
| U-007 | 工作台 | 点击“清空明细录入”，确认弹窗选择确定 | 明细重置为 1 条空白行，汇总值归零，勾选状态清空 |
| U-008 | 弹出录入 | 点击“弹出录入”，在弹窗内新增一行 | 弹窗表格新增行，主表格同步新增 |
| U-009 | 弹出录入 | 在弹窗内点击“清空明细录入”并确认 | 弹窗和主表格均重置为 1 条空白行 |
| U-010 | 客户选择 | 点击客户“选择”，选择已有客户 | 工作台客户字段回填所选客户 |
| U-011 | 客户选择 | 在选择弹窗中新增客户并保存 | 新客户保存并回填到工作台 |
| U-012 | 照片识别 | 上传或拖拽图片 | OCR 状态显示“识别成功”，首行补充识别字段 |
| U-013 | 历史单据 | 切换到“历史单据”，按编号或客户搜索 | 列表仅显示匹配单据 |
| U-014 | 历史单据 | 按日期搜索 | 列表仅显示指定日期单据 |
| U-015 | 统计查询 | 输入产品关键字并调整客户勾选 | 明细和汇总统计按条件变化 |
| U-016 | 统计查询 | 取消全部客户 | 显示“请选择至少一个客户后列出查询明细” |
| U-017 | 客户通讯录 | 新增客户资料并保存 | 客户出现在查询列表中 |
| U-018 | 客户通讯录 | 点击客户列表项 | 表单回填该客户资料 |

## 风险和说明

1. 本次未能实际访问 `http://129.204.142.19/`，远程部署版本是否包含新增按钮需要在网络策略允许后复测。
2. 本地浏览器交互也被策略拦截，因此“清空明细录入”的确认弹窗和 DOM 变化尚未通过浏览器截图验证。
3. 代码层面已通过 TypeScript 诊断和 Vite 构建，当前风险主要集中在浏览器环境和远程部署状态。
